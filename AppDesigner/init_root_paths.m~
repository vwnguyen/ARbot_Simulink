% Usage: change your current working directory to the \v2offset folder
% then run this script, which adds all necessary paths.

% get current working directory
cwd = pwd;
% append necessary folder strings to cwd
Environment = append(cwd,'/Environment');
Workspace = append(cwd,'/workspace_points');
Scripts_Data = append(cwd,'/scripts_data');
Libraries = append(cwd,'/Libraries');

addpath(genpath(Environment));
addpath(genpath(Workspace));
addpath(genpath(Scripts_Data));

% IF YOU NEED TO CHANGE VISUALS
% you can copy the strings generated by these functions

% load STL file paths
base_link = which('base_link.STL');
L1 = which('L1.STL');
L2 = which('L2.STL');
L3 = which('L3.STL');
L4 = which('L4.STL');
% load STEP file paths
chute = which('chute2_default_sldprt.STEP');
guard = which('guard_Default_sldprt.STEP');
shelf = which('shelf_Default_sldprt.STEP');